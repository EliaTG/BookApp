<div class="row formCreate">

    {{!-- {{#if hasRegion}} --}}
    {{!-- {{#if hasType}} --}}
      <div class="card">
        <div class="card-header text-white">
          {{#if editMode}}Edit{{else}}Create{{/if}} a author
        </div>
        <div class="card-body">
          <form method="POST" action="{{#if editMode}}/edit-author{{else}}/create-author{{/if}}">
            <div class="mb-3">
              <label for="name" class="form-label">Name</label>
              <input type="text" class="form-control" id="name" name="Name" value="{{#if editMode}}{{pokemon.title}}{{/if}}" required/>
            </div>
            <div class="mb-3">
              <label for="email" class="form-label">Email</label>
              <input type="email" class="form-control" id="email" name="Email" value="{{#if editMode}}{{pokemon.title}}{{/if}}"  required/>
            </div>


            {{!-- <div class="mb-3">
              <label for="type" class="form-label">Type </label>
              <select class="form-select" id="type" name="TypeId">
                <option {{#if editMode}}{{else}}selected{{/if}}>Select an option</option>

                {{#each type}}

                    {{#if (equalValue type.typeId ../pokemon.typeId )}}
                    <option selected value="{{type.typeId}}">{{type.name}}</option>
                    {{else}}
                    <option value="{{type.typeId}}">{{type.name}}</option>
                    {{/if}}
                  
                {{/each}} 
                
              </select>
            </div> --}}

            {{!-- <div class="mb-3">
              <label for="region" class="form-label">Region </label>
              <select class="form-select" id="region" name="RegionId">
                <option {{#if editMode}}{{else}}selected{{/if}}>Select an option</option>

                {{#each region}}

                    {{#if (equalValue region.regionId ../pokemon.regionId )}}
                    <option selected value="{{region.regionId}}">{{region.name}}</option>
                    {{else}}
                    <option value="{{region.regionId}}">{{region.name}}</option>
                    {{/if}}
                  
                {{/each}} 
                
              </select>
            </div> --}}
  

          {{#if editMode}}

          <input type="hidden" name="authorId" value="{{pokemon.id}}">
          
          {{/if}}

          <button type="submit" class="btn btn-primary">{{#if editMode}}Edit{{else}}Create{{/if}}</button>
           <a href="/authors" type="button" class="btn btn-warning">Back</a>
        </form>

      </div>
    </div>

    {{!-- {{else}} 

    <h1 class="notcreated text-center">There are not pokemon created</h1>

    {{/if}} --}}

</div>